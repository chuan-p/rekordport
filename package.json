{
  "name": "rekordport",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "prepare:frontend": "node tools/prepare-frontend-assets.mjs",
    "dev": "npm run prepare:frontend && vite --host 127.0.0.1 --port 1420",
    "build": "npm run prepare:frontend && vite build",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml",
    "check": "npm run build && npm test",
    "tauri": "node tools/run-tauri.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^5.4.0"
  }
}
